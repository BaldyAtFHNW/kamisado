package kamisado_server;

/**
 * 
 * 	@author Simon Bieri
 * 	@date 2017-08-11
 *	Enum to make sure that the server always uses the correct strings to describe the fields.
 *	As the client never generates any such strings without input from the server we did not include the enums in the client package.
 *
 */
public enum FieldColor {
	BROWN, 
	GREEN,
	RED,
	YELLOW,
	PINK,
	PURPLE,
	BLUE,
	ORANGE
}
